// apps/web/lib/ai/python-client.ts — fetch() wrapper for Python module.
// Functions: parse(), analyze(), analyzeAi(), matchJob(), generate(). Base URL: PYTHON_API_URL, auth: PYTHON_API_KEY.
// Placeholder per CLAUDE.md.
export {};
